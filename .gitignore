# Local secrets and machine state — never commit
.features-secrets.json
.features-kanban.json
.features-workflow.json
.features-models.json
.features-agent-store/
.features-node/

# Installed dependencies (recreate with npm install)
node_modules/

# Environment files that may hold keys
.env
.env.*
!.env.example
